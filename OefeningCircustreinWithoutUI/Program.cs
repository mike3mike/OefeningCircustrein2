using System;
using System.Collections.Generic;

namespace OefeningCircustreinWithoutUI
{
    class Program
    {
        static void Main(string[] args)
        {
/*            List<Animal> animals = new List<Animal>();
            foreach (var foodOption in Enum.GetValues(typeof(Animal.food)))
            {
                foreach(var sizeOption in Enum.GetValues(typeof(Animal.size)))
                {
                    Animal animal = new Animal(foodOption, sizeOption);
                    animals.Add(animal);
                }
            }
            Animal noAnimal = new Animal();
            animals.Add(noAnimal);
            Algorithm algorithm = new Algorithm(animals);*/
        }
    }
}
